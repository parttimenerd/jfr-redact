# Test config with parent = default
parent: default

properties:
  patterns:
    - $PARENT
    - custom_secret
    - my_password

events:
  removed_types:
    - $PARENT
    - jdk.CustomEvent

general:
  pseudonymization:
    enabled: true
    hash_length: 12